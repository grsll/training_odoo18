<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="cdn_pendaftaran_view_list" model="ir.ui.view">
            <field name="name">cdn.pendaftaran.view.list</field>
            <field name="model">cdn.pendaftaran</field>
            <field name="arch" type="xml">
                <list string="">
                    <field name="name"/>
                    <field name="tanggal"/>
                    <field name="pendaftar_id"/>
                    <field name="jenis_kelamin"/>
                    <field name="no_hp"/>
                    <field name="kursus_id"/>
                    <field name="harga_kursus"/>
                    <field name="status_pembayaran"/>
                </list>
            </field>
        </record>
        <record id="cdn_pendaftaran_view_form" model="ir.ui.view">
            <field name="name">cdn.pendaftaran.view.form</field>
            <field name="model">cdn.pendaftaran</field>
            <field name="arch" type="xml">
                <form string="">
                    <header>
                        <button name="action_set_draft" string="Draf" type="object" class="oe_highlight" invisible="state != 'confirm'"/>
                        <button name="action_confirm" string="Confirm" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                        <button name="action_create_invoice" string="Buat Tagihan" type="object" class="oe_link" invisible="state != 'confirm' or invoice_id"/>
                        <field name="state" widget="statusbar" statusbar_visible_levels="draft,confirm"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h3>
                                <label for="name" string="Nomor Pendaftaran"/>
                            </h3>
                            <h1>
                                <field name="name"/>
                            </h1>
                        </div>
                        <group>
                            <field name="tanggal" readonly="state != 'draft'"/>
                            <field name="pendaftar_id" readonly="state != 'draft'"/>
                            <field name="jenis_kelamin" />
                            <field name="no_hp"/>
                            <field name="kursus_id"/>
                            <field name="harga_kursus"/>
                            <field name="status_pembayaran"/>
                        </group>
                        <field name="invoice_id" invisible="1"/>
                    </sheet>
                </form>
            </field>
        </record>
        <!-- cdn_pendaftaran action window -->
        <record id="cdn_pendaftaran_action" model="ir.actions.act_window">
            <field name="name">pendaftaran</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">cdn.pendaftaran</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    <!-- Add Text Here -->
                </p>
                <p>
                    <!-- More details about what a user can do with this object will be OK -->
                </p>
            </field>
        </record>
        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="pendaftaran_menu_act" name="pendaftaran" parent="pendaftaran_menu_categ" action="cdn_pendaftaran_action" sequence="10"/>
    </data>
</odoo>